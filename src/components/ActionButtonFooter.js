import React from "react"
import ActionButton from "./ActionButton"

const ActionButtonFooter = () => (
  <aside>
    <ActionButton className={"d-md-none fixed-bottom w-100 rounded-0"}>
      Entre em contato
    </ActionButton>
  </aside>
)

export default ActionButtonFooter

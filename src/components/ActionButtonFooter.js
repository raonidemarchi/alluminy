import React from "react"
import ActionButton from "./ActionButton"

const ActionButtonFooter = () => (
  <ActionButton className={"fixed-bottom w-100 rounded-0"}>
    Entre em contato
  </ActionButton>
)

export default ActionButtonFooter
